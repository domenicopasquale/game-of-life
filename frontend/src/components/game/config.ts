interface Config {
  GRID_MIN_SIZE: number;
  GRID_MAX_SIZE: number;
  CELL_MIN_SIZE: number;
  CELL_MAX_SIZE: number;
}

export const config: Config = {
  GRID_MIN_SIZE: 10,
  GRID_MAX_SIZE: 100,
  CELL_MIN_SIZE: 10,
  CELL_MAX_SIZE: 50
}; 